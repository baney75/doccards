{
  "name": "doccards",
  "version": "2.0.0",
  "description": "Doc's Cards — Solitaire for Grandpa. A beautiful solitaire collection based on Solitairey.",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l 3000 -s",
    "pages:build": "rm -rf _site && cp -r public _site && echo 'Deployable site ready in _site/'"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
